kind = "web"
previewPath = "/legacy-crm"
title = "Captora CRM (legacy)"
version = "1.0.0"
id = "artifacts/captora-crm"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/legacy-crm" ]
localPort = 26222

[services.development]
run = "pnpm --filter @workspace/captora-crm run dev"

[services.env]
PORT = "26222"
BASE_PATH = "/legacy-crm"
