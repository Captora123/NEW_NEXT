kind = "web"
previewPath = "/"
title = "Captora CRM"
version = "1.0.0"
id = "artifacts/captora-crm"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26222

[services.development]
run = "pnpm --filter @workspace/captora-crm run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/captora-crm", "run", "build" ]
publicDir = "artifacts/captora-crm/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26222"
BASE_PATH = "/"
